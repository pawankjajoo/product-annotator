# Product Annotator

Drop in a product image, place hotspot buttons on it, add titles/descriptions/prices/links per hotspot. Generates embed code for Wix sites.

Canvas API for the editor, supports drag-and-drop repositioning. Also exports CSV and JSON if you need the data elsewhere.
